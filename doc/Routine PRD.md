# Routine PRD

| 项目 | 内容 |
| --- | --- |
| 版本 | v0.2，已同步确认的 V2 页面布局 |
| 日期 | 2026-09-12 |
| 产品 | Friday |
| 关联模块 | [Library](Library%20PRD.md)、Message、Onboarding、Friday Settings |
| 文档范围 | 功能、页面布局、字段、交互、流程与异常状态；本版不包含验收标准 |

## 1. 背景与目标

用户从推荐区添加 Routine，配置需要生成的内容、开始生成时间、格式及推送对象。Friday 按日程生成简报，发送到用户指定的群聊或个人，并将正式生成的文件收录至 Library。

用户只需管理“生成什么、何时生成、发送给谁、使用什么格式”。本 PRD 不定义 Agent 内部检索、工具调用、调度实现或推理策略，也不将规划能力描述成已实现能力。

## 2. 依据与术语

### 2.1 产品依据

- 用户在本次讨论中确认的规则优先于旧 PRD 的冲突内容。
- 已拉取钉钉最新原文：[Message](https://alidocs.dingtalk.com/i/nodes/m9bN7RYPWdbednwAijXE2v45JZd1wyK0)、[Onboarding](https://alidocs.dingtalk.com/i/nodes/mExel2BLV5xpQm0wsp3z7jQRWgk9rpMq)、[Settings](https://alidocs.dingtalk.com/i/nodes/P7QG4Yx2JpEnpRkgiqXmPPaG89dEq3XD)。原文与差异见[同步记录](sources/dingtalk-2026-09-12/README.md)。
- 竞品截图仅作为布局参考。其 Skills、Webhook、分享链接、团队复制、邮件专属配置等不纳入本期。
- 本文标注“建议”的内容尚未经单独确认，其余为已确认规则或对已确认交互的具体展开。

### 2.2 三类模板

| 名称 | 含义 | 关系 |
| --- | --- | --- |
| Routine 模板 | 推荐区中可添加的自动化能力，首发称为“早间简报” | 同一个模板可重复添加 |
| 日程实例 | 用户添加后形成的独立 Routine，拥有自己的内容、时间和推送配置 | 修改或删除一个实例不影响其他实例 |
| 内容模板 | Settings 中用于填入主题与详细 Prompt 的预设 | 共 8 种，不自动修改执行频率 |
| PDF 样式模板 | 控制 PDF 背景和视觉样式的系统预设 | 仅 PDF 可用，不支持用户上传背景 |

首发 Routine 暂沿用用户提出的“早间简报”名称；名称不限制用户设置下午、晚上或每周、每月执行。是否改名为“定时简报”尚未确认。

### 2.3 Connector 与内容边界

- 同一 Friday 用户可同时连接多个不同 Connector，每种 Connector 只连接一个账号；各自的 Routine 可以同时运行。
- 内容由 Prompt 决定，并按需要召回当前用户有权使用的 Memory，不按 Connector 划分取材范围。
- Connector 在 Routine 推送配置中用于选择渠道和收件人；推送渠道不限制内容来源。
- 一个日程可跨多个已连接 Connector，选择多个群聊和个人。
- 同一日程的所有收件人共用相同文件格式；选择 PDF 时共用相同样式模板。
- 定时生成记录由 Recent Sessions 承载，不因定时触发而自动创建一条外部输入 Message。

## 3. 本期范围

| 模块 | 本期内容 |
| --- | --- |
| Routine 首页 | 上方推荐模板、下方我的日程 |
| 添加 | 能力说明、基础配置、确认添加；支持重复添加 |
| 详情 | 左侧实例卡片与操作；右侧 Recent Sessions、Settings、Template 三个 Tab |
| 内容设置 | 8 种内容模板、主题总结、详细 Prompt、双向同步 |
| 时间设置 | 周期、开始生成时间；取材时间范围仅通过 Prompt 控制 |
| 推送设置 | 已连接 Connector、群聊／个人选择、多个目标 |
| 文件设置 | PDF、MD、Word、PNG 四选一 |
| 试运行 | 用当前草稿生成预览，不发送、不进 Library |
| 保存与离开 | 保存正式配置；离开时处理未保存修改 |
| 执行记录 | 生成与各目标发送结果、失败目标重试、文件查看 |
| 删除 | 停止后续执行；保留 Library 历史文件 |

不纳入本期：用户发布 Routine 模板、PDF 背景上传、每个收件人单独设置格式、模板分享市场、Library 文件夹、Agent 内部实现方案。

## 4. 页面信息架构与布局

### 4.1 全局入口

Routine 与 Library 作为 Friday 工作区导航入口，与现有页面共用应用侧栏。沿用 Friday 全局 Settings，不新增独立的账号或 Connector 管理入口。

页面采用已确认的 V2 布局：浅色应用侧栏、白色主画布、黑灰文字、轻边框和细分隔线。卡片负责展示摘要，完整配置和结果在对应详情中查看。主区不堆叠说明、标签与状态，不为了单屏展示压缩字段或字号。

### 4.2 Routine 首页

页面顺序为标题与一句简介 → 推荐 → 我的日程。

```text
应用侧栏 │ Routine
         │ 让重复的工作，按时完成。
         │
         │ 推荐
         │ ┌ 早间简报 ────────┐
         │ │ 图标              │
         │ │ 名称              │
         │ │ 两行能力简介      │
         │ │ [添加 / Add]      │
         │ └───────────────────┘
         │
         │ 我的日程
         │ ┌ 项目周报 ─── ⋯ ┐ ┌ 今日工作简报 ─ ⋯ ┐
         │ │ 一句内容摘要    │ │ 一句内容摘要      │
         │ │ 每周五 18:00    │ │ 每天 09:00        │
         │ └─────────────────┘ └───────────────────┘
```

- 推荐采用独立竖向白色卡片：顶部图标、名称、两行简介，底部固定宽度主按钮。不做横跨页面的 Banner。
- 首发只有一个模板时只放一张卡片，剩余位置留白，不复制卡片凑数。添加过的卡片继续保留 Add，可重复添加。个性化推荐算法不属于本 PRD。
- 我的日程采用较低的摘要卡片网格，宽屏一行可放三张，随可用宽度换行。
- 日程卡片只展示名称、一句内容摘要、周期与开始生成时间、右上角更多菜单；不展示主题标签组、下次执行时间、推送对象数量、渠道或文件格式，也不常驻“修改设置”按钮。
- 点击卡片进入 Recent Sessions；更多菜单提供“修改设置”和“删除”，“修改设置”直接进入该实例的 Settings。
- 首次无日程时显示“还没有日程，从上方添加一个开始”，不展示模拟执行记录。

### 4.3 添加弹窗

参考竞品的“固定标题＋滚动正文＋固定底部操作”，建议宽度约 680–760 px，高度不超过窗口的 85%。

```text
早间简报                                           ×
能力简介
────────────────────────────────────────────────────
如何帮助你
  1. 按主题和 Prompt 整理内容
  2. 按日程开始生成
  3. 向指定对象发送，文件保存在 Library

基础配置
  日程名称
  内容模板
  主题与详细 Prompt
  执行频率／开始生成时间
  Deliver briefing via
  格式
────────────────────────────────────────────────────
                                 [取消] [确认添加]
```

- 名称可编辑，重复添加应有可区分的默认名称，例如“早间简报 2”。此命名方式为建议。
- 主题与 Prompt 交互沿用详情设置；可展开详细 Prompt，减少首屏长度。
- 确认添加后形成独立日程；不会因为添加动作立即生成或发送，按下一次计划时间执行。
- 添加成功后回到首页，我的日程出现新卡片。添加失败保留输入，不创建成功假象。
- PDF 样式建议在详情 Template 中进一步设置；未选择时使用默认 PDF 排版。

### 4.4 日程详情页

三个详情 Tab 共用同一布局：顶部面包屑，左侧固定视觉卡片与操作，右侧标题、简短说明和 Tab 内容。卡片比例、标题起点、Tab 高度和按钮位置在三个页面中保持一致。

```text
Routine > 项目周报

┌ 左侧视觉卡片 ────┐   项目周报
│ 封面             │   每周整理重点工作。
│                  │
│ 实例名称         │   Recent Sessions | Settings | Template
│ 两行简介         │   ─────────────────────────────────────
└──────────────────┘   当前 Tab 内容
   ▶ 试运行
  [  保存  ]
     删除
```

- 左栏为固定比例的视觉封面＋名称＋简短说明，不再重复展示时间、时区、渠道、人数或格式。配置统一留在 Settings。
- 卡片下方纵向提供试运行、保存、删除。保存是唯一实心主按钮，试运行是轻量次操作，删除采用文字操作；不在按钮下堆叠规则说明。
- 保存作用于整个实例的草稿，包括名称、内容、日程、目标、格式和样式，不只作用于当前 Tab。
- 在三个 Tab 间切换保留草稿，不视为离开详情。
- 本期未确认暂停开关，因此不沿用竞品卡片中的启停开关。
- 长内容自然滚动，左侧卡片与操作在桌面窗口中保持可访问；窄窗口可上下排列，不强行缩小字体。

## 5. Settings 功能与字段

Settings 采用可自然滚动的单列长页，顺序为内容配置 → 执行安排 → Deliver briefing via → 格式。模块之间用留白及细分隔线区分，不为每个字段嵌套卡片。

- 内容模板等简单设置行采用“左侧标签、右侧控件”；主题与 Prompt 使用完整行宽，上下排列，不并排挤压。
- 执行频率、执行日、开始生成时间各占一行，标签和必要的一句说明在左，控件在右，不做三列网格。
- 推送区按 Connector 展示简短的已选对象摘要，通过“编辑”打开对象选择弹窗，不常驻逐人表格。
- 格式采用单选下拉框，四个选项为 PDF、MD、Word、PNG，不展开成四块大卡片。
- 实例名称保留可编辑能力；为保持默认页简洁，建议点击详情标题进入名称编辑，不再单独占据常驻表单首行。
- Settings 允许纵向滚动，不要求所有字段同时出现在一个桌面视口内。

### 5.1 内容区

从上到下依次为“内容模板 → 主题总结 → 详细 Prompt”。没有主题模式／Prompt 模式切换。

```text
内容模板       [每周工作总结                ▾]

主题总结       [项目进展] [风险与阻塞] [下周重点]
新增主题       [输入希望关注的主题          ] [Add]

详细 Prompt
┌───────────────────────────────────────────┐
│ 汇总本周的重点工作……                     │
│                                           │
└───────────────────────────────────────────┘
                               [确认 Prompt 修改]
```

| 操作 | 页面结果 | 是否正式生效 |
| --- | --- | --- |
| 选择内容模板 | 填入对应主题及完整 Prompt | 否，写入草稿 |
| 输入主题后点击 Add | 更新主题，并同步修改详细 Prompt | 否，写入草稿 |
| 编辑 Prompt 后点击确认 | 更新草稿 Prompt，重新总结上方主题 | 否，写入草稿 |
| 点击左侧保存 | 保存整个实例配置 | 是，下一次执行使用 |

- Add 和 Prompt 确认是内容同步操作，不等于保存日程。
- 双向同步处理期间显示局部进度；同一时间只提交一个同步操作，避免出现两套不一致的内容。
- 同步失败保留用户输入和上次同步成功的内容，并提供重试；不能显示已同步成功。
- 输入了主题但未 Add，或编辑 Prompt 后未确认时，建议提示先完成同步再试运行／保存，避免使用用户没有看见的另一版内容。
- 已自定义内容后更换内容模板，先提示将替换当前主题与 Prompt；确认后仅替换内容，不改变时间、推送目标、格式或 PDF 样式。
- 主题删除交互尚未单独确认；本版先通过编辑 Prompt 并确认来移除主题，不额外定义独立删除手势。

### 5.2 首期 8 种内容模板

以下为建议的默认主题和 Prompt 文案。均可修改，不构成 Agent 内部执行策略。默认时间范围只是 Prompt 内容，不与日程频率绑定。

| 模板 | 默认主题 | 默认 Prompt |
| --- | --- | --- |
| 每日工作总结 | 今日进展、完成事项、阻塞、下一步 | 请总结今天的工作，按“已完成事项、进行中工作、风险与阻塞、下一步”组织内容。突出重要进展，区分已完成与计划事项。只使用有依据的信息，不编造数据、负责人或期限。 |
| 每周工作总结 | 本周成果、项目进展、风险、下周重点 | 请总结本周工作，按重点项目或工作主题归纳成果、进展、风险和下周重点，避免简单拼接每日记录。区分已经发生的事实与建议行动，不编造数据、负责人或期限。 |
| 每月工作总结 | 月度成果、目标进度、主要问题、下月重点 | 请总结本月工作，整理主要成果、目标进度、关键问题及下月重点。对有依据的目标呈现进展与差距；缺少指标时直接说明，不虚构完成率。 |
| 今日工作简报 | 今日重点、待办、会议、准备事项 | 请整理今天需要关注的工作事项、待办、会议及准备工作，突出有明确截止时间或依赖的行动。优先级必须有依据；不确定的安排需说明，不编造日程。 |
| 项目进展简报 | 重点项目、里程碑、阻塞、下一步 | 请围绕我指定的项目整理近期进展、已完成里程碑、当前风险与阻塞、待协调事项和下一步。未指定项目时，依据已有信息列出当前重点项目；信息不足则明确说明。 |
| 行业洞察 | 行业动态、趋势、影响 | 请围绕我指定的行业总结近期重要动态、趋势与潜在影响，突出变化而非信息堆积。区分事实与分析，重要信息注明可用的时间和来源依据；没有明确行业或资料不足时不要猜测。 |
| 竞品动态简报 | 产品变化、功能更新、定价、市场动作 | 请跟踪我指定的竞品，整理近期产品、功能、定价及市场动作中的实际变化，说明可能的影响。注明已知时间和来源，区分已发布事实与推测；没有指定对象时不要自行编造竞品名单。 |
| 客户跟进简报 | 沟通进展、未答问题、承诺、下一步 | 请围绕我指定的客户整理近期沟通进展、尚未回答的问题、已作出的承诺及下一步。区分客户诉求、我方已确认承诺与建议，不编造负责人或期限。 |

模板选择器建议显示 8 个选项及一句描述，无须另建完整内容模板市场。行业、竞品、项目、客户等关注对象通过主题或 Prompt 调整，不新增额外业务配置表单。

### 5.3 日程设置

| 字段 | 行为 |
| --- | --- |
| 执行频率 | 建议首期支持每天、每周、每月 |
| 星期 | 每周显示，可选择一周中的执行日；是否多选尚未确认，建议首期单选 |
| 日期 | 每月显示；建议提供 1–28 日及“每月最后一天”，避免无对应日期的歧义 |
| 开始生成时间 | 时间选择器。含义为开始生成，不承诺在该时间送达 |
| 时间说明 | 显示“将在所选时间开始生成，完成后自动发送” |
| 时区 | 建议默认使用创建时用户时区，并显示具体时区，避免出差后静默改变日程 |

- 不提供取材时间范围字段；取材时间范围完全由 Prompt 控制。
- 选择“每周工作总结”不会自动把频率改成每周。
- 新配置从下一次执行生效；已经开始的执行保持启动时配置。
- 未保存草稿不影响原日程，它仍按上次保存配置执行。

### 5.4 Deliver briefing via

先选择已连接 Connector，再弹窗选择发送对象。

```text
Deliver briefing via
[选择已连接的渠道 ▾]

钉钉   项目组、陈思睿                    [编辑]
Teams  产品组                            [编辑]
```

收件人弹窗布局：

```text
选择钉钉收件人                                      ×
[群聊] [个人]
[搜索群聊或姓名]
□ 名称               可用的组织／成员等辅助信息
□ 名称               可用的辅助信息
已选择：若干对象
                                    [取消] [确认选择]
```

- 已连接 Connector 均可选择；通过群聊／个人 Tab 查找并多选对象。
- 名称相同的对象展示可供辨认的辅助信息，不让用户凭名字猜测。
- 确认选择后汇总到 Settings，以渠道名称和对象摘要展示；点击“编辑”重新打开选择弹窗，可增删该渠道的群聊和个人，也可继续添加其他 Connector。
- 同一 Connector 下的同一目标去重；跨 Connector 的对象分别显示。
- 目标列表加载失败提供重试，不显示模拟对象；取消弹窗不修改原有目标。
- 无已连接 Connector 时提示去 Friday 全局设置连接。
- 建议正式保存／添加至少要求一个有效推送目标；试运行不需要推送目标。
- 多目标共用格式和 PDF 样式；需要不同格式时，重复添加一个独立日程。

### 5.5 文件格式

- 单选 PDF、MD、Word、PNG；Word 指可下载的 `.docx` 文件，MD 指 `.md` 文件。
- 格式作用于同一次正式生成及其全部推送目标。
- 建议默认 PDF；用户选择其他格式时保留其内容配置。
- 文件较长时 PNG 的分页／多图规则尚未确认，不默认承诺一张无限长图。

## 6. Template Tab

- 右侧默认只展示“选择 PDF 样式”、一句说明和样式缩略图网格；宽屏一行展示三款，更多样式自然换行。
- 每款样式只显示文档缩略图、名称和选中状态，不常驻大幅预览、格式徽标或重复的“已选择”说明。
- 选择样式更新草稿选中态；缩略图预览入口打开按需放大的预览层，关闭后回到样式列表。预览不等于保存，正式生效仍通过左侧保存。
- 非 PDF 时用提示和“切换为 PDF”按钮替代整个样式选择区；PDF 状态下不再同时显示该提示。

- PDF 格式下展示系统预设样式缩略图、样式名称、预览与选中状态。
- 样式控制视觉呈现，不改变内容主题、Prompt 或执行时间。
- 不支持上传背景或自定义模板编辑器。
- 未选择样式时建议采用默认 PDF 排版。
- 非 PDF 时显示“切换为 PDF 后可使用样式模板”，提供“切换为 PDF”按钮；切换写入草稿，保存后才正式生效。
- 从 PDF 切换其他格式时，建议保留原样式选择但不应用；切回 PDF 后恢复选择。
- 系统样式的具体数量、名称和素材由设计补齐，不在本版虚构已上线样式。

## 7. 试运行、保存与离开

### 7.1 试运行

1. 使用当前同步完成的草稿内容及所选格式／样式生成预览，无须先保存正式配置。
2. 点击后展示生成中状态，防止重复提交。
3. 完成后打开预览层，显示生成文章内容；关闭后回到详情并保留草稿。
4. 不发送给任何对象，不改变日程，不纳入 Library。它是正式文件全收录规则的例外。
5. 生成失败或资料不足时显示原因并允许返回修改；不展示空白文件为成功结果。
6. 建议 Recent Sessions 仅列正式执行，试运行结果保留在当前预览中，避免与正式发送记录混淆。

### 7.2 保存

- 保存整个实例配置；有未确认的 Prompt 编辑或主题同步进行中时先提示完成同步。
- 保存成功后草稿成为正式配置，后续执行使用新配置。
- 保存失败保留草稿及原正式配置，用户可重试。
- 已经开始的执行不受本次保存影响。

### 7.3 有未保存修改时离开

仅在用户准备离开详情时弹出，不在每次字段修改时打断。

```text
有未保存的修改                                     ×
保存后，后续日程才会使用当前配置；
未保存时仍按原配置执行。

                              [取消保存] [保存]
```

| 操作 | 结果 |
| --- | --- |
| 取消保存 | 放弃本次全部未保存修改，恢复原配置，并继续原定离开动作 |
| 保存 | 保存成功后继续离开；校验或保存失败则保留草稿，停留详情 |
| × | 关闭弹窗，留在编辑页面，保留未保存修改 |

返回首页、切换到 Library／Message 等页面属于离开；切换详情内部 Tab 不属于离开。应用关闭是否能拦截取决于宿主能力，不承诺无法实现的关闭保护。

## 8. Recent Sessions

### 8.1 列表

按开始生成时间倒序展示正式执行记录。默认采用轻量列表，每行只展示时间、标题、一项结果摘要及进入箭头；用细分隔线区分，不为每条记录套大卡片。

默认不展开文章正文、文件块、收件人、失败原因或重试按钮。点击某条记录后才进入该次执行详情，查看下节内容；返回后保留列表位置。失败结果可使用克制的提示色，其余状态以文字表达。

未执行过时显示“日程执行后，你可以在这里查看生成内容和发送结果”。

### 8.2 单次记录详情

- 顶部：日程名称、本次开始时间、触发类型和结果摘要。
- 正文：生成内容预览或文件入口；正式文件可跳转 Library 查看。
- 推送结果：按 Connector 分组，逐个显示群聊／个人及其结果。
- 失败时提供可读原因和适用的重试操作。

| 情况 | 显示与操作 | Library |
| --- | --- | --- |
| 正在生成 | 显示生成中 | 未形成文件前无条目 |
| 资料不足 | 记录“本次资料不足，已跳过发送” | 不新增文件 |
| 生成失败 | 显示失败原因 | 未生成文件则不新增 |
| 已生成，正在发送 | 展示文件及发送进度 | 已收录 |
| 全部发送成功 | 显示已发送及目标结果 | 保留文件 |
| 部分发送失败 | 展示逐目标结果，允许仅重试失败目标 | 保留原文件 |
| 全部发送失败 | 展示原因，允许重试失败目标 | 保留原文件 |

- 重试发送复用原文件，不能重新生成或再次发送给已成功的目标。
- 发送结果未知时应明确显示“结果待确认”，不能直接视为失败而重复发送；处理方式留给执行能力定义。
- 同一次生成向多个目标发送只对应同一个文件，不因目标数或重试次数重复收录。

## 9. 删除日程

- 首页更多菜单和详情左侧删除按钮指向同一操作。
- 建议显示确认弹窗：“删除此日程后将停止后续执行，Library 中已有文件会保留。”
- 确认删除后从我的日程移除，不影响同一模板创建的其他实例。
- Library 已生成文件保留，来源标注“原日程已删除”。
- 删除时若已有执行正在进行，其继续生成／发送还是终止尚未确认；本版不将停止后续执行等同于撤销已经开始的动作。

## 10. 关键完整流程

### 添加并正式执行

打开 Routine → 推荐卡片 Add → 阅读说明 → 选择内容模板并调整主题／Prompt → 配置开始时间、对象和格式 → 确认添加 → 我的日程出现实例 → 到时开始生成 → 正式文件进入 Library → 向各目标发送 → Recent Sessions 记录各目标结果。

### 修改与试运行

我的日程卡片更多菜单 → 修改设置 → 更新主题或 Prompt 并完成同步 → 试运行 → 查看预览 → 返回继续修改 → 保存 → 下一次执行使用新配置。试运行不发送、不归档。

### 部分发送失败

文件生成并归档 → 部分对象发送失败 → 查看本次执行详情 → 重试失败目标 → 复用原文件 → 更新失败目标结果，已成功目标不重复发送。

## 11. 尚待确认的细节

这些项目不会覆盖已确认的主流程：

- “早间简报”是否改为更宽泛的名称。
- 每月日期、每周多日、时区以及应用不在线时的执行规则。
- 保存是否必须有推送对象，是否允许仅生成并归档的正式日程。
- 删除时已经开始的执行如何处理。
- 全局免打扰是否影响 Routine 推送；已确认的开始时间含义保持不变。
- PNG 长内容输出方式、系统 PDF 样式清单。
- 全局完成通知入口：云端 Onboarding 的时钟／铃铛规则仍有冲突。本版以 Recent Sessions 承载执行结果，不自行扩展通知约定。
