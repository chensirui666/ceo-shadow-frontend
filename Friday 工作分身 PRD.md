# Friday 工作分身 PRD

| 项目 | 内容 |
| --- | --- |
| 版本 | V1.0 |
| 日期 | 2026-08-04 |
| 产品阶段 | 基于现有 CEO Agent 能力的产品化 MVP |
| 核心入口 | 钉钉工作消息；本机运行、可审计、先试运行后启用 |

## 1. 一句话定义

Friday 是一个驻留在管理者工作流里的工作分身：它依据被授权的工作资料和本人工作画像，先在 **Draft mode** 中理解、起草和记录工作消息；用户确认后，才在限定范围内协助处理真实消息。

它不是一个新的聊天工具，也不是通用项目管理软件。它的核心价值是：让管理者可以看见、纠正并逐步启用一个“像自己工作”的分身。

## 2. 立项依据与事实边界

本 PRD 以现有 CEO Agent 服务为能力基线，而不是以讨论中的概念为基线。以下状态必须在产品和研发中保持一致。

| 能力 | 现有代码事实 | PRD 决策 |
| --- | --- | --- |
| 钉钉消息 | 已有消息读取、@ 触发、回复、文档/知识库读取、审批/日历等执行链路，以及 dry run 和 live-send 门禁 | **V1 核心渠道**。对用户承诺的“连接应用”只先落到钉钉。 |
| 钉钉任务 | 已能从处理过的对话、会议纪要和本地资料形成项目、TODO、更新和跟进草稿；可同步部分高置信 TODO 到钉钉 Todo | **保留**，抽象为 Tasks，而非另做项目管理工具。 |
| 工作画像 | 已会从本地工作资料、AI 听记、已发送的钉钉消息和可读知识库中生成本地 `work profile`；运行时只消费画像，不直接暴露原始证据 | **保留并产品化为 Memory 的“工作画像”**。 |
| 审计/事件 | 已保存处理尝试、生成草稿、发送状态、错误、重试、外部动作和会议记录 | **保留**，产品化为 Home 的事件流；不暴露内部日志格式、会话 ID 或工具调用。 |
| 回复反馈 | 已能记录收件人对已发送回复的评分/评语，也有内部处理与“已解决”状态 | **保留**，产品化为 Feedback；当前不承诺反馈会自动重训或立即改写工作画像。 |
| 微信 | 已有个人微信本机链路；当前以“生成后人工批准/拒绝发送”为主要模式，依赖 macOS 辅助功能与独立发送组件 | **不纳入 V1 自助连接**。作为 P1 的受控渠道，先保持人工发送审核。 |
| 飞书 | 现有能力仅覆盖通过 `lark-cli` 只读获取文档材料 | **不作为消息连接器**；后续可作为 Memory 的只读资料源。 |
| Gmail / Slack | 没有第一方 OAuth、消息读取、事件订阅或用户设置入口。现有邮件动作属于 DWS 执行能力，不能等同于 Gmail 集成 | **不展示为“可连接”**，列为后续渠道项目。 |
| 历史 AI 对话迁移 | 未找到 ChatGPT、Codex、Claude Code 的终端用户导入与清洗链路 | **不进入首次引导**；Memory 的后续能力，另立需求后再实现。 |

### 2.1 本期产品范围

**V1 要交付**：钉钉连接、工作画像建立与预览、真实消息的 Dry Run、显式启用、事件首页、任务视图、Memory 视图、反馈闭环和安全设置。

**V1 不交付**：Slack/Gmail/飞书消息接入、通用二维码连接器、历史 AI 对话导入、知识图谱、可编辑 Prompt、自动根据每条反馈重训、泛项目协作、独立聊天窗口。

这不是收缩概念，而是避免用户在第一天就看到“可连接”却无法真正使用的渠道。

## 3. 目标用户、问题与成功标准

### 3.1 目标用户

以钉钉为主要工作入口、频繁被群聊 @ 的管理者或负责人。他需要分身先处理重复沟通，但不愿把对外承诺、重要判断和隐私资料交给黑盒自动化。

### 3.2 要解决的问题

1. 工作消息来得快，管理者无法逐条及时回应。
2. 通用 AI 不理解项目背景，也不像本人表达和判断。
3. 自动回复风险高：用户不知道它读了什么、写了什么、发了什么。
4. 待办和后续跟进散落在消息、会议和个人脑中。

### 3.3 北极星结果

用户在一次简短设置后，能亲眼看到 Friday 对真实工作消息给出的草稿，给出一次有效反馈，并在自己明确启用后让分身开始处理限定范围内的消息。

### 3.4 成功指标

| 指标 | 定义 |
| --- | --- |
| 首次价值达成率 | 完成连接并看到第一条 Dry Run 草稿的新增用户占比。 |
| 设置操作时长 | 从进入 Home 到完成三个设置动作的中位时长；目标 ≤ 3 分钟。等待真实 @ 消息、后台资料处理不计入操作时长。 |
| 启用转化率 | 完成 Dry Run 后主动点击“正式启用”的用户占比。 |
| 草稿可用率 | 用户标记“可用”或仅轻微编辑后认可的草稿占比。 |
| 安全质量 | Active 模式下，每个外发动作都可回溯到触发消息、生成草稿、最终状态和用户规则。 |

## 4. 核心原则

1. **先展示真实结果，再请求自动化权力。** 默认 Trial，绝不在首次设置中直接外发。
2. **一个主渠道先跑通。** V1 只让用户连接真正可用的钉钉，不能把“规划中”伪装成按钮。
3. **用户看结果，不看系统内部。** 让用户看到消息、摘要、草稿、任务和状态；隐藏路径、环境变量、Prompt、模型会话与原始 JSON。
4. **Memory 是可理解的工作记忆，不是技术名词。** 用户能知道资料来源、工作画像和刷新时间；默认不展示原始语料或知识图谱。
5. **外发必须可控。** Trial 不发送；Active 必须由用户明确开启；高风险或信息不足时交回本人。
6. **反馈必须形成闭环。** 任何一条“不像我”的反馈都应能关联到具体草稿；是否写入工作画像必须显式呈现，不能暗中宣称已学习。

## 5. 完整用户旅程

### 5.1 获客到登录

用户在一位真实使用 Friday 的管理者博主视频中看到完整片段：同事在钉钉群里 @ 博主；Friday 引用已有工作背景生成草稿；博主说“先说结论，别替我承诺”，草稿随即被修订。视频链接跳转到 Friday 落地页，用户下载桌面端并通过邮箱验证码登录。

落地页只承诺已经存在的路径：**连接钉钉 → 建立工作画像 → 先试运行，再决定是否启用。** 不把 Slack、Gmail、飞书消息接入作为当前卖点。

### 5.2 登录后的 Contextual Onboarding

用户首次登录进入 Home。页面顶部显示一个 **Contextual Onboarding / Onboarding Checklist（情境式引导清单）**：它嵌入日常首页，只在尚未完成设置时出现，完成后自动淡出，Home 回归为事件首页。

引导不是四个独立页面，而是三步进度：

1. 连接应用并建立 Memory
2. 生成并确认工作风格
3. Dry Run 并启用

这属于 **渐进式披露（Progressive Disclosure）**：用户只需要做当前一步，不需要先理解所有设置模块。

### 5.3 步骤 1：连接应用并建立 Memory

#### 页面目的

完成一个可真实工作的钉钉连接，并基于已授权、可读的工作资料创建第一版工作画像。

#### 页面文案与组件

| 区域 | 内容与文案 | 行为 |
| --- | --- | --- |
| 标题 | `连接你的工作入口` | 副标题：`先从一个你每天都在用的应用开始。Friday 只会在你授权的范围内协助处理工作。` |
| 应用卡片 | `钉钉 · 已支持`；说明：`处理群聊中 @ 你的工作消息，并读取关联的可授权工作资料。` | 主按钮：`连接钉钉`。授权完成后显示 `已连接`。 |
| 连接范围 | `默认处理范围：群聊中 @ 我的消息` | 不把 `@所有人` 作为触发条件。私聊、审批、日历等扩展动作不在首次引导中默认开启。 |
| 资料确认 | `建立我的工作记忆`；说明：`Friday 会从你已授权且可读取的工作资料中整理项目背景、决策习惯和回复样本，用来生成工作画像。` | 单一确认按钮：`确认并建立`。不要求用户逐份挑选、上传或配置。 |
| 隐私说明 | `本机工作资料与工作画像保存在此设备的 Friday 工作区。只有你另外启用的外部记忆服务，才会按其授权范围写入。` | 点击 `了解数据范围` 打开来源类型和外部记忆连接状态，不承诺所有数据永远只在本地。 |
| 完成态 | `第一版工作画像已准备好` | 次按钮：`预览 Memory`；主按钮：`继续生成工作风格`。 |

#### 产品规则

- 系统资料来源可以包括本地工作资料、可读钉钉知识库、AI 听记和用户已有的钉钉回复样本；具体来源不足时仍可用安全的初始画像进入下一步。
- 用户可跳过“预览 Memory”，但不能跳过已授权范围和 Trial 默认值。
- 当前不在这里展示“迁移 ChatGPT / Codex / Claude Code 历史对话”。该能力进入 Memory 页的“后续增强”，未实现前不出现假入口。
- 授权 UI 可以采用钉钉 OAuth 或由安装器承接的官方授权流程；现有代码没有可直接复用的通用二维码连接页，因此“扫码即连”是后续连接器体验的实现项，不能写成已具备能力。

### 5.4 步骤 2：生成并确认工作风格

#### 页面目的

把底层的工作画像转成用户能核对、能理解的“分身工作方式”。

#### 页面文案与组件

| 区域 | 内容与文案 | 行为 |
| --- | --- | --- |
| 标题 | `这是 Friday 对你工作方式的第一版理解` | 副标题：`它会用这套规则生成草稿；信息不足或涉及重要决定时，会交还给你。` |
| 工作方式摘要 | 三个固定小节：`回复方式`、`判断优先级`、`必须交给我` | 例如：`先说结论，再给原因和下一步`；`材料不足时先追问`；`审批、承诺、人事与客户关键判断先交给我`。内容来自当前 work profile 的可读摘要。 |
| 证据范围 | `基于：已授权工作资料、会议记录、历史工作回复` | 只显示来源类型和更新时间，不展示原始私人文本或内部证据 ID。 |
| 操作 | 主按钮：`确认并进入试运行`；次按钮：`返回查看 Memory` | V1 为“确认使用”。逐条编辑规则、人工改写画像作为后续迭代，不伪装成当前能力。 |

#### 产品规则

- 画像生成完成后提供预览，而不是展示“模型正在学习”的不可解释 loading。
- 当前“确认”表示允许 Friday 在 Dry Run 中使用该画像；不等同于允许发送。
- 若画像或资料构建尚未完成，显示 `正在准备第一版工作画像，你仍可先查看已连接的应用状态。`，不阻塞其他页面。

### 5.5 步骤 3：Dry Run 并启用

#### 页面目的

让用户看到真实消息如何被理解和起草，并以内容反馈验证是否像本人。

#### 页面文案与组件

| 区域 | 内容与文案 | 行为 |
| --- | --- | --- |
| Trial 状态条 | `试运行中：Friday 会生成草稿和记录，但不会向同事发送消息。` | 始终显示在页面顶部，直到用户启用 Active。 |
| 等待态 | `等待第一条符合范围的钉钉消息`；说明：`当有人在群聊中 @ 你时，Friday 会自动生成第一条草稿。` | 这是当前可验证的真实路径；不承诺虚构样本能在三分钟内到达。 |
| 草稿卡 | `对方说了什么`、`Friday 理解的背景`、`建议回复`、`处理建议` | 处理建议只用用户语言：`可直接回复`、`需要你确认`、`资料不足，建议追问`。 |
| 草稿反馈 | 快捷反馈：`像我会说的话`、`再简短一点`、`更友好一点`、`不要替我承诺`、`这种情况先问我`；文本框占位：`告诉 Friday 哪里不对` | 反馈必须绑定这条草稿并可回看。V1 先保存为回复审核反馈；“自动写入工作画像并重生成”需要单独实现后才开放。 |
| 启用确认 | 标题：`准备好让 Friday 协助回复了吗？`；列出：`渠道：钉钉`、`范围：群聊中 @ 我`、`当前工作画像版本`、`高风险情况仍会交给我` | 主按钮：`正式启用`。点击后才可进入 Active；二次确认后写入真实运行开关。 |

#### 产品规则

- Trial 继续读取、判断、生成草稿并保留审计，但禁止外部发送。这与现有 dry-run 语义一致。
- Active 也不表示“所有消息自动回复”。高风险、权限不足、材料不足或明确需本人决定的情形必须转为 `需要你确认`。
- 用户在未收到真实 @ 消息前可以离开 Home；下次回来仍看到 Trial 等待态。为满足“3 分钟完成”而提供的可重放样本，需要另做安全的模拟/回放能力，不能用当前真实外发路径冒充。

## 6. 信息架构

一级导航固定为：**Home · Tasks · Memory · Feedback · Settings**。

| 页面 | 用户的问题 | 页面职责 | 明确不承担 |
| --- | --- | --- | --- |
| Home | `分身今天做了什么，我需要处理什么？` | 首次引导、状态总览、事件流与待我确认事项 | 聊天入口、技术审计后台、项目管理。 |
| Tasks | `工作里有哪些项目、待办与下一步？` | 从消息/会议/资料沉淀的项目与 TODO 视图 | 手工协作看板、替代钉钉 Todo。 |
| Memory | `分身凭什么这样判断，它记住了什么？` | 工作画像、来源范围、刷新状态、后续记忆能力 | 原始语料浏览器、知识图谱、未经实现的导入器。 |
| Feedback | `哪些回复表现不好，怎么处理？` | 聚合对回复的真实评价与内部审核结论 | 自动训练黑盒、面向全公司的满意度调研。 |
| Settings | `哪些应用、规则和安全开关生效？` | 连接、运行模式、边界、隐私与通知 | 环境变量、Prompt 编辑、模型/工具/数据库配置。 |

## 7. 页面详细需求

### 7.1 Home

#### 目标

Home 是分身的“今日运行面板”。新用户在这里完成引导；已启用用户在这里看今天发生的事和必须由自己决定的事。

#### 页面状态

| 状态 | 必须显示 | 不显示 |
| --- | --- | --- |
| 首次设置 | 顶部三步 Onboarding Checklist；当前步骤的完整内容；下方精简事件空状态 | 完整的配置后台、未支持渠道卡片。 |
| Trial 等待消息 | Trial 状态条、首次草稿等待态、已连接渠道和 Memory/画像状态 | `正式启用` 按钮。 |
| Trial 有草稿 | 最新草稿卡、草稿反馈、`正式启用` 确认入口 | 自动发送操作。 |
| Active | 今日待确认、最近 24 小时事件流、运行状态 | 已完成的引导清单。 |
| 已暂停/连接异常 | 醒目的状态说明、`前往 Settings 处理` | 误导性的“分身正在工作”。 |

#### Active 页内容

1. **顶部状态栏**
   - `Friday 正在协助你处理钉钉工作消息` 或 `Friday 已暂停`。
   - 状态标签：`Trial`、`Active`、`需要处理连接`。
   - 次级信息：当前渠道、处理范围、工作画像最近更新时间。
   - CTA：`查看设置`；Trial 下为 `继续试运行`。

2. **需要你确认**
   - 只显示现有运行链路能产生的高价值事项：需要人工 handoff 的消息、微信确认发送（P1 渠道时）、失败的外部动作、需要本人判断的事项。
   - 每项显示：来源应用、对象/会话、发生时间、一句原因、`查看详情`。
   - 空状态：`目前没有需要你亲自处理的事项。`

3. **最近 24 小时事件**
   - 事件类型：消息进入处理、草稿生成、已发送/未发送、转交本人、任务新建/更新、会后对齐、外部动作执行、处理失败。
   - 每条仅显示：会话或项目名称、简短输入/输出摘要、状态、时间、`查看详情`。
   - 详情沿用现有处理尝试与任务记录，但产品页隐藏原始审计 JSON、Codex session、错误堆栈和数据库路径。
   - 空状态：`还没有工作事件。连接钉钉后，Friday 会从下一条符合范围的消息开始记录。`

4. **今日工作摘要（P1）**
   - 仅当事件数据已稳定时显示：`已处理`、`待你确认`、`形成任务` 三项数量。
   - 不在 P0 伪造“效率提升”“节省时间”等无法从现有记录可靠计算的指标。

#### Home 验收

- 新用户只看见一个当前引导步骤，不需要在五个页面中找开始入口。
- 完成启用后，引导清单不再占据首页；事件流自然上移，这是“完成态渐隐/Conditional Onboarding”的行为。
- 用户从任一事件可进入可读详情，知道是否发送、为何需要自己处理。

### 7.2 Tasks

#### 目标

将已经从工作对话、会议纪要和资料中形成的事项，以项目为主线呈现，让用户知道下一步和阻塞点。

#### 列表页

| 模块 | 内容 |
| --- | --- |
| 标题 | `Tasks`；副标题：`从工作消息、会议和资料中持续整理的项目与待办。` |
| 筛选 | `全部`、`进行中`、`已逾期`、`已完成`；可按项目分类、负责人、优先级搜索。 |
| 项目行 | 项目名、当前状态、负责人、优先级、风险、下一步、开放待办数、最近更新。 |
| 排序 | 默认 `需要关注优先`；支持按优先级、风险、下一步时间、负责人排序。 |
| 空状态 | `暂时没有沉淀出需要跟进的工作事项。Friday 会在处理消息和会议后持续整理。` |

#### 项目详情

按以下顺序显示：

1. 项目概览：目标、当前状态、下一步、阻塞、负责人、更新时间。
2. TODO：标题、状态、负责人、优先级、截止时间、下一次跟进、阻塞/完成依据。
3. 跟进：已经形成的跟进草稿及其状态；只在已有数据时展示。
4. 更新记录：用自然语言显示“什么时间、发生了什么变化、为什么”，不显示置信度、变更 JSON 或内部 source ID。
5. 来源：关联的会话或会议标题，点击后进入对应事件详情。
6. 钉钉 Todo 同步：只显示 `已同步`、`同步中`、`同步失败` 与失败提示；不把外部 task id、pull/push 时间当成主内容。

#### 关键决策

- `work_todos` 是 Friday 的工作主数据，钉钉 Todo 是执行同步层；产品不能反过来把 Tasks 设计成钉钉 Todo 的镜像。
- 不在 V1 提供“新建项目”“拖拽看板”“多人评论”——现有系统的价值是自动沉淀和跟进，做成泛协作工具会稀释主线。
- 不自动为每条 TODO 建钉钉任务：现有规则只对明确负责人、截止时间、非敏感且高置信的事项同步，产品需要保持这一边界。

#### Tasks 验收

- 每个项目都至少能回答：目标是什么、谁负责、下一步是什么、卡在哪里。
- 每个 TODO 的状态和来源可追溯；同步失败不会被隐藏。

### 7.3 Memory

#### 目标

让用户理解分身使用的工作依据和当前工作画像，同时把“本机资料”和“外部长期记忆”明确区分。

#### 页面结构

1. **工作画像**
   - 标题：`Friday 目前如何理解你的工作方式`。
   - 显示：画像版本/更新时间、回复方式、判断优先级、需要交还给本人处理的边界。
   - CTA：`查看完整画像`。完整画像以可阅读文档呈现，不展示 prompt 注入格式或证据 ID。

2. **记忆来源**
   - 标题：`用于建立这版画像的资料范围`。
   - 来源类型：本地工作资料、会议/AI 听记、钉钉历史工作回复、钉钉可读知识库。
   - 每类仅展示状态、最近更新时间和简短说明，如：`只读，用于补足项目背景`。
   - 无资料时清楚显示 `尚未发现可用于工作画像的资料，Friday 正在使用安全的初始画像。`

3. **刷新与复核**
   - P0：显示最近构建状态、是否成功、`查看构建结果`。
   - P1：`刷新工作画像`，需要后台把现有 corpus 与 profile 生成流程封装成用户动作后才可开放。
   - P1：`申请调整工作方式`，将用户在 Dry Run/Feedback 的已确认建议汇总为候选变更，用户审核后写入画像。

4. **长期记忆连接（可选）**
   - 只在已配置外部 Memory Connector 时显示：`已连接`、授权状态、最近写入状态和 `前往设置`。
   - 未配置时显示 `未连接。Friday 仍可使用本机工作画像。`
   - 不在这里暴露外部服务的账户 ID、MCP 配置、图谱 ID 或隐私数据。

5. **历史 AI 对话迁移（后续能力）**
   - 目标能力：从 ChatGPT、Codex、Claude Code 导入用户主动选择的工作历史，先识别个人/非工作内容，再由用户确认哪些主题可进入 Memory。
   - 当前状态：未实现；V1 只在产品路线图/空位说明中表达，不放可点击上传按钮。

#### 隐私文案规范

- 可以说：`本机工作资料与工作画像保存在本机 Friday 工作区。`
- 不可以笼统说：`所有 Memory 完全只在本地。` 因为当前服务可选接入外部 Memory Connector，也会从已授权的线上资料读取内容。
- 外部资料一律用“已授权、只读、来源类型”描述，不使用“读取你所有文档”。

#### Memory 验收

- 用户能在 30 秒内判断：分身目前依据了哪些类型的工作资料、画像何时更新、是否使用外部长期记忆。
- 页面不出现原始训练样本、私人对话、技术路径或未经验证的“知识图谱”。

### 7.4 Feedback

#### 目标

把“这条回复好不好、哪里不对”从散落的链接和人工备注，变成可处理、可回溯的质量闭环。

#### 列表页

| 模块 | 内容 |
| --- | --- |
| 标题 | `Feedback`；副标题：`来自收件人和你的回复质量反馈。` |
| 默认视图 | `待处理` 优先；可切换 `全部`、`已处理`。 |
| 反馈行 | 评分/标签、评语、关联会话和触发消息摘要、生成回复摘要、来源、时间、状态。 |
| 操作 | `查看这条回复`、`标记已处理`。已有收件人反馈可以执行同步。 |
| 空状态 | `还没有收到回复反馈。Friday 会把对已发送回复的评价集中在这里。` |

#### 反馈详情与处理规则

- 点击进入对应事件详情，用户同时看到原消息、当时草稿/最终回复和所有评价。
- 用户自身在 Dry Run 中给出的“更简短”“别替我承诺”等意见，也应作为同一种“回复审核反馈”存储，并标明来源为 `我`，不与收件人评分混淆。
- V1 的“标记已处理”只表示用户已看过并完成处理，映射现有 resolved 状态。
- P1 才提供 `加入工作画像候选规则`：用户必须在看到候选规则后确认写入。未实现前，不得在 UI 中说“反馈已自动让分身更懂你”。

#### 明确舍弃

- 不把现有“未反馈次数达到阈值后提醒/阻断”的内部策略做成面向用户或同事的产品规则；它会把产品从协作体验变成催评分机制。
- 不暴露反馈 token、外部反馈 URL、同步请求错误细节。

#### Feedback 验收

- 每条反馈都可追溯到具体回复；用户能分清“同事评价”和“我自己的审核意见”。
- 未处理反馈在 Home 和 Feedback 中都可被发现，但不会干扰日常事件流。

### 7.5 Settings

#### 目标

让用户管理真实生效的连接、运行边界和安全状态，而不是进入工程配置页。

#### 分区与内容

| 分区 | 内容 | V1 操作 |
| --- | --- | --- |
| 应用与连接 | 钉钉连接状态、当前处理范围、授权状态；微信仅在 P1 受控渠道开通后显示 | `重新连接`、`暂停连接`、查看授权范围。 |
| 运行模式 | `Trial` / `Active` 状态、最近状态变化时间、当前渠道范围 | Trial 下可 `继续试运行`；Active 下可 `暂停分身`。从 Trial 到 Active 必须再次确认。 |
| 回复边界 | 群聊只在 `@ 我` 时处理；高风险/材料不足时交给我；当前工作画像版本 | V1 仅展示生效边界。允许用户配置“回复等待时间”等规则前，必须先有后端真实支持。 |
| 数据与隐私 | 本机工作资料和画像状态、已授权资料类型、可选外部长期记忆连接状态 | 查看数据范围、前往 Memory。全量删除/导出须有安全实现后才上线。 |
| 通知与诊断 | 连接异常、处理失败、需要本人确认的通知开关和最近状态 | P1；当前仅在系统已有可靠通知渠道后开放。 |

#### Settings 中明确不出现的内容

- Developer Prompt、User Prompt、环境变量、数据库路径、模型会话、MCP server、CLI 安装状态、LaunchAgent、内部路由规则、原始日志。
- 这些是工程运维面，不是 CEO/管理者的产品设置。若保留，另设受限的内部 Admin Console，不作为 Friday 用户界面。

#### Settings 验收

- 用户在一个页面内能回答：现在是否真的会发送、处理哪个渠道/范围、出问题去哪里看、如何暂停。
- 任意“启用发送”操作都不可由刷新页面、后台任务或未确认的默认值触发。

## 8. 状态模型与发送门禁

| 产品状态 | 行为 | 外发 |
| --- | --- | --- |
| 未连接 | 不读取工作消息，不处理 | 禁止 |
| 已连接 / 正在建立画像 | 可准备资料，不对外处理 | 禁止 |
| Trial 等待 | 等待第一条符合范围的真实消息 | 禁止 |
| Trial 有草稿 | 读取、判断、生成、审计、收集反馈 | 禁止 |
| Active | 依据范围处理消息；高风险或资料不足转交本人 | 仅在明确 live-send 门禁已开启时允许 |
| 暂停/异常 | 停止新处理，保留历史可查看 | 禁止 |

产品 UI 的 `正式启用` 必须同时满足：用户完成确认、服务端 live-send 门禁满足、渠道连接健康。任一条件不满足时，状态必须停留在 Trial 或暂停，并说明原因。

## 9. 关键交互与文案清单

| 场景 | 推荐文案 | 禁止文案 |
| --- | --- | --- |
| 连接钉钉 | `连接钉钉，开始建立你的工作分身` | `连接全部工作应用` |
| 资料建立 | `Friday 会从你已授权且可读取的工作资料中建立第一版工作画像。` | `我们会读取你的所有资料。` |
| 本机隐私 | `本机工作资料与工作画像保存在此设备；外部长期记忆仅在你单独连接后使用。` | `所有数据永远只在本地。` |
| Dry Run | `只生成草稿，不会向同事发送。` | `已自动回复，但你可以撤回。` |
| 高风险处理 | `这件事需要你确认后再回复。` | `Friday 已代表你答应。` |
| 反馈 | `告诉 Friday 这条草稿哪里不像你。` | `已自动学习并永久记住。` |
| Active | `正式启用后，Friday 将在钉钉群聊中协助处理 @ 你的消息。` | `从现在开始自动处理所有工作。` |

## 10. 版本规划

### P0：现有能力的产品化

- 钉钉作为唯一自助主渠道。
- Home 三步 Contextual Onboarding、Trial/Active 状态与事件流。
- Tasks 项目与 TODO 的可读业务视图。
- Memory 工作画像、来源范围、构建状态。
- Feedback 收件人评价/审核反馈的聚合与已处理状态。
- Settings 的连接、运行模式、安全边界视图。

### P1：把现有局部链路变成用户能力

- 微信本机连接包装与人工批准发送界面。
- 工作画像刷新、基于反馈的候选规则审核。
- 飞书作为只读 Memory 资料源的授权与可见状态。
- Home 的今日数量摘要与可靠通知。
- 安全的 Dry Run 样本回放，使用户不必等待下一条真实 @ 消息。

### P2：新渠道和历史迁移

- Gmail/Slack 的独立 OAuth、读取、事件订阅、范围控制、Trial 与发送门禁。
- ChatGPT、Codex、Claude Code 历史工作对话迁移及个人内容过滤/用户确认。
- 资料删除、导出和更细粒度的 Memory 管理能力。

## 11. 不做清单

1. 不在首页保留 `History` 一级页面；历史记录合并为 Home 事件流。
2. 不提供 `My Work Avatar` 独立页面；工作画像属于 Memory。
3. 不做通用聊天页；分身价值应发生在真实工作渠道，而非再建一个对话孤岛。
4. 不让用户在产品设置里编辑 Prompt、环境变量或模型参数。
5. 不承诺未实现的 Slack、Gmail、飞书消息自动回复或一键二维码连接。
6. 不把所有 Memory 都描述为本地，也不默认读取全部文档。
7. 不把 Task 做成替代钉钉或 Linear 的完整项目管理系统。

## 12. 上线验收清单

### 首次体验

- [ ] 用户从登录进入 Home 后，能在不阅读帮助中心的情况下完成钉钉连接、确认资料建立、查看工作画像。
- [ ] 引导只显示三步；完成后从 Home 消失。
- [ ] 用户未点击“正式启用”前，没有任何外发动作。
- [ ] 用户能看到 Trial 下第一条真实草稿及其反馈入口。

### 五个页面

- [ ] Home 只展示当前状态、待确认事项和可读事件；无内部日志与技术配置。
- [ ] Tasks 的项目和 TODO 均来自现有工作沉淀链路，且能看到下一步/阻塞/来源。
- [ ] Memory 准确区分本机画像、已授权资料与可选外部长期记忆。
- [ ] Feedback 可将评分/评语关联回具体回复，支持标记已处理。
- [ ] Settings 可安全地查看和暂停实际运行范围，且不暴露工程配置。

### 风险与真实性

- [ ] 渠道列表只展示真实可用能力；飞书、Gmail、Slack 不会以已连接状态误导用户。
- [ ] 每个外发动作具有可读的触发来源、草稿和最终发送状态。
- [ ] 任何“反馈已生效”的提示都有明确对应：仅已记录、待审核，或已确认写入画像。
- [ ] 任何“本地”隐私文案都不覆盖用户主动连接的外部服务。

## 13. 需要产品负责人确认的唯一范围决策

本 PRD 将 **钉钉定为 V1 唯一自助连接渠道**，因为这是现有代码中唯一完整的“接收消息 → 生成/发送 → 审计 → 任务”闭环。若产品目标必须在 MVP 同时展示并连接企业微信、飞书、Gmail、Slack，则那不是 UI 文案调整，而是至少四条新的连接器与发送门禁研发项目；应在立项时单独排期，而不是在 Home 上先展示出来。

## 附录 A：实现溯源（研发使用）

| PRD 主题 | 已核对的实现依据 |
| --- | --- |
| 渠道、Trial/Active 与任务主数据 | `README.md` 的本地优先运行、钉钉/飞书说明、dry-run/live-send 门禁与任务同步说明。 |
| Home 事件和处理详情 | `app/audit_web.py` 的处理尝试、待处理任务、会议事件、微信人工发送审核和详情视图。 |
| Tasks | `app/audit_web.py` 的项目/待办列表与详情；`app/store.py` 的项目、TODO、更新、跟进草稿与钉钉 Todo 同步记录。 |
| Memory / 工作画像 | `app/corpus.py` 的会议/历史回复语料，`app/work_profile.py` 的画像构建和运行时规则，`app/memory_setup.py` 的可选外部长期记忆配置。 |
| Feedback | `app/feedback_events.py` 的收件人评分同步，`app/audit_web.py` 的反馈列表和 resolved 状态，`app/feedback_policy.py` 的内部提醒/阻断策略。 |
| Settings 的舍弃项 | `app/audit_web.py` 现有 Prompt、环境变量、路由和微信目标配置页；本 PRD 将其与用户可见设置明确分离。 |

后续每次新增渠道、外部发送动作或 Memory 数据路径时，必须先更新本表和第 2 节事实边界，再修改用户界面与宣传文案。
