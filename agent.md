# 项目基本信息

- **项目名称**：Friday 工作分身前端。
- **项目目标**：完成 Friday 工作分身的前端项目。
- **原项目**：[callzhang/ceo-agent-service](https://github.com/callzhang/ceo-agent-service)。本项目以其现有能力为基础，完成面向用户的产品化前端，而非重建后端服务。
- **已有资料**：doc下的文件夹 为产品需求资料，`DESIGN.md` 为设计规范。
- **设计要求**：所有 UI 修改前先阅读并遵守 [DESIGN.md](DESIGN.md)。

## 技术栈

- **前端框架**：React 19 + TypeScript。
- **构建工具**：Vite 8。
- **样式方案**：Tailwind CSS 4，通过 `@tailwindcss/vite` 接入。
- **组件库**：HeroUI 3。
- **质量检查**：`tsc --noEmit` 类型检查；Node 内置 `node:test` 测试。

## 编程原则

1. 每个文件保持单一职责；尽量组件化，组件保持模块化，关注点分离。
2. 在满足当前需求的前提下选择最简单的实现；不要预防性抽象或无必要的配置层。
3. 先跑通最小端到端版本，再逐步扩展；不要为尚未发生的复杂度拆掉已经能跑的实现。
4. 优先使用成熟、持续维护的库；没有明确理由不自行重写。
5. 先检查项目已有依赖是否可用，再考虑新增依赖或自行实现。
6. 架构决策面向长期；不接受“先这样，以后再换”的临时方案。
7. 先研究成熟产品处理同类问题的方式，优先采用已验证的模式，不从零发明。

## 思维模型
始终从第一性原理出发去思考问题本来的样子。
